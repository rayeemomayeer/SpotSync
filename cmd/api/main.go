// Package main is the SpotSync API entrypoint.
// Wiring is added in Phase 0 step 3+.
package main

import (
	"fmt"
	"os"
)

func main() {
	fmt.Println("SpotSync API — scaffold placeholder (Phase 0 step 1)")
	os.Exit(0)
}
