// Package main seeds an initial admin user (Phase 0 step 10+).
package main

import (
	"fmt"
	"os"
)

func main() {
	fmt.Println("SpotSync seed — scaffold placeholder (Phase 0 step 10+)")
	os.Exit(0)
}
