// Package main is the SpotSync release worker entrypoint (Phase 1+).
package main

import (
	"fmt"
	"os"
)

func main() {
	fmt.Println("SpotSync worker — scaffold placeholder (Phase 1+)")
	os.Exit(0)
}
