# Kubernetes (kind)

Local/CI portfolio proof with kind — not a paid cluster. Manifests and scripts under this folder support multi-replica experiments alongside Redis.

Staging on free-tier Render/Neon/Vercel: see [../staging.md](../staging.md).
